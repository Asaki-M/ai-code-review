{
  "name": "@ai-code-review/api",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm --filter @ai-code-review/core build && tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-code-review/core": "workspace:*",
    "@hono/node-server": "^1.14.1",
    "hono": "^4.7.8"
  }
}
