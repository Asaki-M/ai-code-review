# 多 Agent 开发实践

## 起点：手撸一个多 Agent 玩具

最开始，我手撸了一版简单的多 Agent 聊天和任务编排的小玩具：

- 仓库地址：[Asaki-M/mutil-agent-server](https://github.com/Asaki-M/mutil-agent-server)

整体比较粗糙，主要做了两块：

- 多 Agent 聊天
- 自定义任务编排服务

### 多 Agent 聊天

多 Agent 聊天是通过事件订阅的方式实现的。

在这个模式里，不同的 Agent 可以设置不同的系统提示词，用来给它们配置不同的人设。然后再由用户消息驱动它们进行对话，体验上有点类似“斗蛐蛐”。

不过这一版只做了短期的滑动窗口记忆管理，还缺少很多关键能力：

- 没有完整的状态管理
- 没有做长期记忆管理

我对长期记忆的预期设计是：先总结消息记录，再存到向量数据库里，然后由一个专门的记忆 Agent 来负责这件事。

不过这个方案还没有真正尝试过。社区里也看到过一些观点，认为用 RAG 做长期记忆其实并不一定很好。但因为我目前做的几个 Agent 玩具还没有真正用上长期记忆，所以这块也就还没实践和深入理解。

### 自定义任务编排服务

自定义任务编排服务是通过定义有向图来处理的。

Tool 的使用方式选了两种：

- HTTP 调用
- 本地 Node 脚本路径

因为我只是在本地跑，所以本地 Node 脚本的形式可以接受。这里不会有危险脚本的使用问题，因为有向图和工具都是我自己定义的。

不过这一版同样比较粗糙：

- 没有做状态管理
- 多个 Agent 只能按照顺序一个一个地跑
- 没有更完整的并发、恢复和调度能力

## LangGraph 的使用

回到现在这个仓库，它是一个用 LangGraph 做多 Agent 编排的 Code Review CLI 工具。

主要目的有两个：

- 学习 LangGraph 里的多 Agent 任务编排是怎么做的
- 学习 LangGraph 的状态管理是怎么设计的

也顺带用一下成熟框架，避免一直手撸代码、闭门造车。

使用方面没什么特别复杂的地方：把文档丢给 AI 就行了。功能写完之后，记得让 AI 标注出文档出处，然后再对照文档和实际使用方式，确保没有用错。

## LangGraph 状态管理

LangGraph 的状态管理，本质上是定义一个 `State`，用来存储多个 Agent 在工作流里的状态数据。

节点不会直接随意修改整份状态，而是通过返回局部更新来修改状态。每个字段还可以通过 `channel` 配置自己的合并规则，也就是指定这个字段应该如何被更新：

- 完全覆盖
- 拼接合并
- 使用自定义 `reducer` 合并

如果有多个节点同时修改同一个字段，LangGraph 会先把这些更新收集起来，然后再统一按照对应的 `reducer` 处理数据。

不过这里有一个需要注意的问题：这些数据的顺序不一定是有序的。如果业务上要求顺序稳定，需要自己再做一次 `sort`。

我这个项目里的流程目前都是串行的，所以不需要额外处理排序问题。

另外，LangGraph 还有一些更完整的能力，例如：

- 持久化恢复
- 中断恢复
- Checkpoint

这些功能我目前都还没用到，所以也还没深入研究。后续有时间可以继续看看原理，并尝试把它们加到这个 workflow 里面。

一句话总结：

> LangGraph 里的 `State` 本质上是一份在整个工作流中共享的状态对象。节点通过返回局部更新去修改它，而每个字段又可以通过 `channel` / `reducer` 定义自己的合并规则。

## LangGraph 任务编排

LangGraph 的任务编排，本质上是通过一个有向图来驱动工作流。

整体流程可以理解为：

1. 先定义一些节点 `Node`
2. 每个节点本质上是一个函数
3. 节点函数接收当前的 `State`
4. 节点内部处理对应事件
5. 节点返回一个局部更新，用来更新 `State`
6. 通过边 `Edge` 把节点连接起来
7. 工作流按照有向图的结构执行

其中，普通的 `Edge` 用来描述固定流转关系。

如果需要条件分支，可以使用 `ConditionalEdges`。它里面会有一个路由函数，用来决定下一步应该跳转到哪个节点。最后再传入一个路由映射表，把路由函数的返回值映射到具体节点。

这样的设计可以解耦判断逻辑和节点名称。当然，如果不需要这层映射，也可以直接在路由函数里写节点名称。

### 关于循环

既然工作流是有向图，那么它也可以包含循环。

比如我这里的逻辑大概是：

```text
review => modify => review => commit ...
```

在我的实现里，`review` 之后会有一个手动选择逻辑，用来决定是否打破循环。

但如果循环没有被打破，工作流就会一直执行下去。这种情况下，这个流程就不是一个好的编排。

如果确实需要循环多次，建议在 `State` 里面维护一个循环次数变量：

1. 每次循环时累加次数
2. 达到上限后打破循环
3. 避免工作流无限执行

### checkpoint 组件持久化和恢复中断

- 持久化：就是用本地数据库来存储数据，有好几款数据库的 langgraph 里面有提供对应的插件包
- 恢复和中断：根据存在数据库的数据来恢复，主要依赖 `thread_id` 来当主键

具体流程在创建 `workflow` 任务编排时候传入一个 `checkpoint` 配置项来开启快照保存，在执行到每一个节点时候都会做一个保存快照的操作，像 thread_id, state 等等的数据根据 checkpoint 的存储方式来存入对应的数据库，然后中断的时候就已经保存了快照，下次恢复时候根据对应的 `thread_id` 恢复快照，然后继续根据任务编排的有向图来执行。

在这个项目我在 cli 需要选择操作时候有用过和手动中断 cli 的时候能够根据 `thread_id` 恢复

在多个节点并发的时候 channel 修改还没修改完就中断的话，checkpoint 会保存这些还没修改完的 channel 等再次恢复后再将未修改的 channel 继续修改到快照的对应 channel。但是这块的功能我就没去实践试，因为当前项目并没有并发节点的操作。

